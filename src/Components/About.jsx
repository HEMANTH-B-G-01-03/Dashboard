import { Container, Typography } from "@mui/material";

const About = () => (
  <Container>
    <Typography variant="h4">About Page</Typography>
    <Typography>This is the About page of our React App.</Typography>
  </Container>
);

export default About;
